@namespace("controllers.protocols")
protocol Points {

  record Point {
    double x, y;
  }

  void addPoint(Point point);

  void clear() oneway;

  array<Point> getNearestPoints(Point from, int k);
}
